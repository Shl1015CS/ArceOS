[package]
name = "arceos-std-webdemo"
version = "0.1.0"
edition.workspace = true
authors = ["eternalcomet <mydragonos@outlook.com>"]

[target.'cfg(target_os = "hermit")'.dependencies]
arceos-rust = { workspace = true, default-features = true, features = ["multitask", "net", "fs", "log-level-debug"] }

